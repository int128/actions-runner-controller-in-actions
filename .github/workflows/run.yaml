name: run

on:
  pull_request:

jobs:
  actions-runner-controller:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v3
      - run: helmfile init
      - run: bash setup.sh
